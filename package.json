{
  "name": "trench",
  "version": "0.1.0",
  "description": "trench — linear holder-airdrop order platform (Solana). Devs open an order, pick a supply amount and target cap, and a bot drops tokens to holders #1–#200 as the market cap doubles.",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "db:init": "node scripts/init-db.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.21.2",
    "pg": "^8.13.1"
  }
}
